{
  "name": "@capire/incidents",
  "version": "1.0.0-20251106084150+83c3274f739dc7c8a66229e9ce2736fa2bd96cfc",
  "dependencies": {
    "@cap-js/hana": "^2",
    "@sap/cds": ">=8",
    "@sap/xssec": "^4",
    "express": "^4"
  },
  "devDependencies": {
    "@cap-js/cds-test": "*",
    "@cap-js/audit-logging": ">=0.8.3",
    "@cap-js/change-tracking": "^1.0.6",
    "@cap-js/attachments": "^2",
    "@cap-js/sqlite": ">=1"
  },
  "scripts": {
    "watch": "cds watch",
    "start": "cds-serve",
    "jest": "npx jest --silent",
    "test": "chest",
    "production-build": "npm rm @sap/cds && npm add @sap/cds-dk && cds build --production",
    "ui-build": "mkdir -p app/html5-deployer/resources && npm install --prefix app/incidents && npm --prefix app/incidents run build && cp -f app/incidents/dist/incidents.zip app/html5-deployer/resources/incidents.zip"
  },
  "sapux": [
    "app/incidents"
  ],
  "private": true,
  "cds": {
    "requires": {
      "[production]": {
        "auth": "xsuaa",
        "db": "hana"
      },
      "destinations": true,
      "html5-repo": true,
      "portal": true
    }
  }
}
